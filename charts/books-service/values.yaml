replicaCount: 2

image:
  repository: docker.io/brayanprado09/practica7-microservicios-books
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3002

env:
  DB_HOST: "postgresql"
  DB_USER: "admin"
  DB_NAME: "booksdb"
  PORT: "3002"

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"

securityContext:
  runAsNonRoot: true
  runAsUser: 1001
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false

probes:
  enabled: true

nodeSelector: {}
tolerations: []
affinity: {}